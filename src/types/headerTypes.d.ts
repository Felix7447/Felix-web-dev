export interface Header {
  menu: {
    about: string
    projects: string
    skills: string
    contact: string
  }
}

export interface HeaderMenu {
  about: string
  projects: string
  skills: string
  contact: string
}